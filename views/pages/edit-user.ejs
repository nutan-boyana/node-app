<div class="container-fluid">
    <div class="row">
        <!-- Sidebar -->
        <div class="col-md-3 col-lg-2 bg-dark text-white min-vh-100 p-3">
            <%- include('../includes/sidebar', { user: user }) %>
        </div>
        <div class="col-md-3 col-lg-10 p-4">
            <h2>Edit User</h2>

            <!-- <h2 class="mb-4">Edit User</h2> -->

            <form id="editForm" class="p-3 border rounded bg-light shadow-sm">

                <!-- Name -->
                <div class="mb-3">
                    <label class="form-label">Name</label>
                    <input name="name" value="<%= user.name %>" class="form-control" placeholder="Enter name">
                </div>

                <!-- Email -->
                <div class="mb-3">
                    <label class="form-label">Email</label>
                    <input name="email" value="<%= user.email %>" class="form-control" placeholder="Enter email">
                </div>

                <!-- Role -->
                <div class="mb-3">
                    <label class="form-label">Role</label>
                    <input name="role" value="<%= user.role %>" class="form-control" placeholder="Enter role">
                </div>

                <!-- Department -->
                <div class="mb-3">
                    <label class="form-label">Department</label>
                    <select name="department_id" class="form-control">
                        <option value="">Select Department</option>
                        <% departments.forEach(dept => { %>
                            <option value="<%= dept.department_id %>" <%= user.department_id === dept.department_id ? 'selected' : '' %>><%= dept.department_name %></option>
                        <% }); %>
                    </select>
                </div>

                <!-- Button -->
                <button type="submit" class="btn btn-primary w-100">
                    Update User
                </button>

            </form>
        </div>
    </div>
</div>

<script>
    document.getElementById('editForm').onsubmit = function (e) {
        e.preventDefault();

        fetch('/update-user/<%= user.id %>', {
            method: 'PUT',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({
                name: this.name.value,
                email: this.email.value,
                role: this.role.value,
                department_id: this.department_id.value
            })
        })
            .then(res => res.json())
            .then(data => {
                if (data.status === 'success') {
                    window.location.href = '/manage-users';
                } else {
                    alert('Update failed');
                }
            })
            .catch(err => {
                console.error(err);
                alert('Server error');
            });
    };
</script>